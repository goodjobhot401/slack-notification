name: Github Actions Repo

on:
  push:
    branches:
      - main

jobs:
  slackNotification:
    runs-on: ubuntu-latest
    steps:
      - name: Send Notification to Slack
        uses: rtCamp/action-slack-notify@v2

        env:
          SLACK_COLOR: "good"
          SLACK_ICON: https://s3.amazonaws.com/pix.iemoji.com/images/emoji/apple/ios-12/256/ghost.png
          SLACK_TITLE: "A new commit has been pushed into main branch"
          SLACK_USERNAME: "Github Action 小精靈"
          SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK }}
